/** @type {import('next').NextConfig} */
const nextConfig = {
    // Simple config
    reactStrictMode: false, // Turn off strict mode for map rendering ease
    eslint: {
        ignoreDuringBuilds: true,
    },
};

export default nextConfig;
